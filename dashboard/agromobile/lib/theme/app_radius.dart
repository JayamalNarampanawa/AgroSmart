import 'package:flutter/material.dart';

class AppRadius {
  const AppRadius._();

  static const double card = 20;
  static const double button = 16;
  static const double chip = 14;
  static const double modal = 24;
  static const double nav = 28;

  static BorderRadius cardRadius = BorderRadius.circular(card);
  static BorderRadius buttonRadius = BorderRadius.circular(button);
  static BorderRadius chipRadius = BorderRadius.circular(chip);
  static BorderRadius modalRadius = BorderRadius.circular(modal);
  static BorderRadius navRadius = BorderRadius.circular(nav);
}
